# Copy to .env.development (or export inline) to run against offline fixture
# data instead of the live MangaDex API — required inside sandboxes that
# block api.mangadex.org.
#
#   VITE_USE_FIXTURES=1 npm run dev
VITE_USE_FIXTURES=1
